package com.example.levarehulticops.dto;

/**
 * DTO for representing an employee’s name in WorkOrder contexts.
 */
public record EmployeeDto(
        /** Full name of the employee */
        String name
) {}
